import './GalleryImageCard.css';

const GalleryImageCardSkeleton = () => {
  return (
    <div className="gallery-image-card skeleton-card">
    </div>
  );
};

export default GalleryImageCardSkeleton;